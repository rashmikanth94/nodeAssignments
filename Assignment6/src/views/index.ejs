<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%=title%></title>
</head>
<body>
    <form action="/postBug" method="POST">
        <div class="form-group">
            <div>
                <label>Title</label>
                <input type="text" name="title" class="form-control">
            </div>
            <div>
                <label>Description</label>
                <input type="text" name="description" class="form-control">
            </div>
            <div>
                <label>Assignee</label>
                <input type="text" name="assignee" class="form-control">
            </div>
        </div>
        <div>
            <button type="submit">Submit</button>
        </div>
    </form>
    <div>
        <h3>Status</h3>
        <table>
            <thead>
                <th>Title</th>
                <th>Description</th>
                <th>Assignee</th>
                <th>Start Date</th>
                <th>Expiry Date</th>
                <th></th>
            </thead>
            <tbody>
                <%for(let i=0; i<data.length;i++){%>
                    <tr>
                        <td><%=data[i].title%></td>
                        <td><%=data[i].description%></td>
                        <td><%=data[i].assignee%></td>
                        <td><%=data[i].createdDate%></td>
                        <td><%=data[i].ExpiryDate%></td>
                        <td>
                            <a href="/closeBug/<%= data[i]._id %>">Close Bug</a>
                        </td>
                    </tr>
                <%}%>
            </tbody>
        </table>
    </div>
</body>
</html>